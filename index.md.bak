---
layout: default
title: Home
---

<div class="hero">
  <h1>ELM GWANDARU</h1>
  <p>Echoes of fire, silence, and thought — poems stitched into existence.</p>
</div>

## Latest Poems
<ul>
  {% for post in site.posts %}
    <li>
      <a href="{{ post.url | relative_url }}">{{ post.title }}</a> 
      <small>({{ post.date | date: "%B %d, %Y" }})</small>
    </li>
  {% endfor %}
</ul>
